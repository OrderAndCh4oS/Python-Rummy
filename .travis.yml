language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7"
  - "3.8"
install:
  - pip install --upgrade pip
  - pip install poetry
  - poetry install -v
# command to run tests
script:
  - coverage run -m pytest
# run coverage
after_success:
  - codecov
